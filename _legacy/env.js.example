// Local template only. Copy to "env.js" for LOCAL testing and fill in your values.
// On GitHub, env.js is generated automatically from repo Secrets by the deploy
// workflow — do NOT commit a real env.js (it is gitignored).
window.SUPABASE_URL  = "https://YOUR-PROJECT.supabase.co";
window.SUPABASE_ANON = "YOUR-ANON-KEY";  // anon/public key only — never the service_role key
